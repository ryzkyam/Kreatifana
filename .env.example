# Database connection URL (ini untuk backend, tidak perlu di frontend jika backend terpisah)
# DATABASE_URL="postgresql://username:password@localhost:5432/creativehub?schema=public"

# JWT Secret for authentication (ini untuk backend, tidak perlu di frontend jika backend terpisah)
# JWT_SECRET="your-jwt-secret-key"

# Vite specific env variables
# PERBAIKAN PENTING DI BAWAH INI
